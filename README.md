Setup Instruction-----
Follow the steps below to run the PayFlex UI project locally.

1️ Clone the repository
git clone https://github.com/anshul1606/Pay-Flex.git

2️ Go into the project folder
cd Pay-Flex

3️ Install dependencies (if any in future)
For now, your project uses pure HTML, CSS, JS, so no installation is required.
If you later add npm features:
npm install

4️ Open the project in VS Code
code .

5️ Start the project

Since it’s a static website, you can open the index.html file directly:
Option A — Using VS Code Live Server (recommended)
Install the Live Server extension in VS Code
Right-click index.html → Open with Live Server
The site will open at: http://localhost:5500/


Technologies, Frameworks & Libraries Used
Frontend----
HTML5
CSS3 (Flexbox, Grid, Animations, Variables)
JavaScript (ES6+)

Libraries----
Swiper.js — Sliders & carousels
Google Fonts (Inter) — UI typography
IntersectionObserver API — Scroll-based reveal animations
LocalStorage API — Theme persistence (Dark/Light mode)

UI/UX Enhancements----
SVG blob hero animation
Glassmorphism card UI
Sticky feature highlights
Animated counters
Gradient CTA buttons
Smooth auto-scrolling brand slider
Floating chat/CTA button

Assets----
Fintech illustrations
3D support icon
Brand logos (SVG/PNG)
Custom payment graphics
